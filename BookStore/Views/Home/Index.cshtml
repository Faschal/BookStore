@{ 
    ViewData["Title"] = "Home";
}

<section class="jumbotron text-center">
    <div class="container">
        <h1>Welcome to Bookstore</h1>        
        <p class="lead text-muted">
            Welcome to the Bookstore web application
        </p>
        <p>
            <a href="#" class="btn btn-primary my-2">Search Book</a>
        </p>
    </div>
</section>

<div class="py-5 bg-light">
    <div class="container">
        @*<h3 class="h3">Top Books</h3>*@
        <big>Top books</big>
        
        <div class="row">
            <div class="col-md-4">
                <partial name="_bookThumbnail" model="new Book()"/>                
            </div>

            <div class="col-md-4">
                @await Html.PartialAsync("_bookThumbnail", new Book());
            </div>

            <div class="col-md-4">
                @await Html.PartialAsync("_bookThumbnail", new Book());
            </div>
        </div>
    </div>
</div>

